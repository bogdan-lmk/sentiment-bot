from setuptools import setup, find_packages

setup(
    name="ai-santiment",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List your requirements from requirements.txt here
    ],
    python_requires=">=3.9",
)
